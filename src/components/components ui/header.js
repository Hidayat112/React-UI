import React from 'react';
import logo from '../../breaking bad/breaking logo.png';

const Header = () => {
    return (
        <header className='center'>
            <img src={logo} alt=''/>
        </header>
    )
}

export default Header
